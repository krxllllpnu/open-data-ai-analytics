name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-module:
    name: Run ${{ matrix.module }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - module: data_load
            script: data_load/app.py
            prepare_data: false
          - module: data_quality
            script: data_quality_analysis/app.py
            prepare_data: true
          - module: data_research
            script: data_research/app.py
            prepare_data: true
          - module: visualization
            script: visualization/app.py
            prepare_data: true

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create artifacts directory
        run: mkdir -p artifacts

      - name: Prepare dataset
        if: matrix.prepare_data == true
        run: python data_load/app.py > artifacts/prepare-data.log 2>&1

      - name: Run selected module
        run: python ${{ matrix.script }} 2>&1 | tee artifacts/${{ matrix.module }}.log

      - name: Collect generated outputs
        run: |
          mkdir -p artifacts/reports artifacts/plots
          cp -r reports/* artifacts/reports/ 2>/dev/null || true
          cp -r plots/* artifacts/plots/ 2>/dev/null || true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.module }}-artifacts
          path: artifacts/
