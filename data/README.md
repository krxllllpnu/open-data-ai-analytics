# Data Directory

This directory stores local data files used by the analysis.

- `data/raw/` is ignored by Git and can contain downloaded source files.
- `data/processed/` is created by `src/data_load.py` and contains small processed outputs used by the scripts.
