apiVersion: v1
kind: Service
metadata:
  name: open-data-ai-analytics-web
  namespace: open-data-ai-analytics
  labels:
    app.kubernetes.io/name: open-data-ai-analytics-web
    app.kubernetes.io/part-of: open-data-ai-analytics
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: open-data-ai-analytics-web
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
